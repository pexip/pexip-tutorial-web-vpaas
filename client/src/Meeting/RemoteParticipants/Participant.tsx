import { Cell } from '@pexip/components'
// TODO (02) Import the new dependencies from the @pexip/components package
// TODO (03) Import the type Cells from the @pexip/components package

// TODO (04) Define the Participant props interface

// TODO (05) Define the component props
export const Participant = (): JSX.Element => {
  return (
    <Cell className="Cell">
      {/* TODO (06) Display the participant's video */}
      {/* TODO (07) Display the participant's panel when there is no video */}
      {/* TODO (08) Display the participant's audio */}
    </Cell>
  )
}
